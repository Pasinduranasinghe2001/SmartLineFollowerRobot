; EC6090 Mini-Project - PlatformIO Configuration
; Board: ESP32 DevKit (38-pin)

[env:esp32dev]
platform  = espressif32
board     = esp32dev
framework = arduino

monitor_speed  = 115200
monitor_filters = direct
upload_port  = ${env.UPLOAD_PORT_DEV}
monitor_port = ${env.MONITOR_PORT_DEV}

lib_deps =
    madhephaestus/ESP32Servo@^0.13.0

build_flags =
    -DCORE_DEBUG_LEVEL=0
    -DBOARD_HAS_PSRAM=0
